import {Settings} from './Settings.js';

export class Cells{
    #settings = new Settings();

    generateCells(rows = this.#settings.difficulty[0].rows, cols = this.#settings.difficulty[0].cols, mines = this.#settings.difficulty[0].mines){
        
    }
}